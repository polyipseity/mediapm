{
  "known_failures": [
    {
      "test": "output::progress::tests::sync_hierarchy_complex_resize_scenario",
      "crate": "mediapm",
      "message": "assertion `left == right` failed: restored line count matches original (left: 3, right: 4)",
      "notes": "Pre-existing progress bar resize test failure, unrelated to cache changes"
    }
  ]
}
