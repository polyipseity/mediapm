//! Builtin command implementations exposed by the `mediapm` executable.
//!
//! These handlers are invoked through `mediapm builtins ...` and may also be
//! reached through compatibility internal command routes used by generated tool
//! launchers.

pub mod media_tagger;
