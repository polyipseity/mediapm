//! `export` builtin — filesystem materialization.
//!
//! Impure builtin that writes payloads to the host filesystem.
//! Registered as `builtins.export@1.0.0`.

pub use mediapm_conductor_builtin_export::{TOOL_ID, TOOL_NAME, TOOL_VERSION};
