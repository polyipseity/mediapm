//! `fs` builtin — filesystem operations.
//!
//! Impure builtin that reads, writes, and lists filesystem entries.
//! Registered as `builtins.fs@1.0.0`.

pub use mediapm_conductor_builtin_fs::{TOOL_ID, TOOL_NAME, TOOL_VERSION};
