//! `archive` builtin — content transformation (pack/unpack/repack).
//!
//! Pure builtin that transforms bytes between raw, ZIP, and canonical
//! uncompressed-ZIP representations. Registered as `builtins.archive@1.0.0`.

pub use mediapm_conductor_builtin_archive::{META, TOOL_ID, TOOL_NAME, TOOL_VERSION};
