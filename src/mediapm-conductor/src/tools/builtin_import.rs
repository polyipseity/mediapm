//! `import` builtin — source-ingestion operations.
//!
//! Impure builtin that reads files, folders, URL content, or CAS entries.
//! Registered as `builtins.import@1.0.0`.

pub use mediapm_conductor_builtin_import::{TOOL_ID, TOOL_NAME, TOOL_VERSION};
