//! `echo` builtin — returns text as stdout/stderr string-map.
//!
//! Pure builtin that echoes input text back. Registered as
//! `builtins.echo@1.0.0`.

pub use mediapm_conductor_builtin_echo::{TOOL_ID, TOOL_NAME, TOOL_VERSION};
