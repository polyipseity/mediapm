//! Standalone runner for the archive builtin crate.
//!
//! `archive` is pure and transforms input bytes into output bytes.

mediapm_utils::builtin_main_single_writer!(mediapm_conductor_builtin_archive);
